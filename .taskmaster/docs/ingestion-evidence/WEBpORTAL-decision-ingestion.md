# WEBpORTAL Decision Document — Full No-Skim Ingestion Record

Source file: `/home/ubuntu/upload/WEBpORTAL.docx`
Extraction read: `/home/ubuntu/ai-coding-workshop-permanent/.taskmaster/docs/ingestion-evidence/WEBpORTAL-extracted-paragraphs.md`
Read status: **Full extracted document read from line 1 through line 1009, including all paragraph IDs P001 through P229.**

## Document Identity

This document records a clarification conversation about what **My AI Coding Workshop / AI-API-Web-Portal-v2** must become after a prior implementation was found misaligned with the owner’s intended product. The document repeatedly distinguishes the required product from a single-AI command composer, provider picker, raw terminal UI, or generic workspace. The agreed target is a **Manus-like, task-first wrapper portal** in which the owner writes in plain English, Claude and Kimi are orchestrated behind the scenes, CLI activity is translated in real time, memory is global, files are task-scoped with a master file view, and credentials are global/owner-only.

The final instruction in the document is especially strong: the owner requires a **real working orchestration MVP**, production-ready behavior, 100% adherence to the decisions made in the session, and a proper architecture/planning phase before writing code.

## Line-Referenced Requirement and Decision Extraction

| ID | Source Lines / Paragraphs | Requirement / Decision | Implementation Implication To Audit Later |
|---|---:|---|---|
| WP-001 | 5-20 / P001-P004 | The normal workflow must translate an English request into a plan for approval, execute it, and translate results back to English. The owner should not see raw terminal or raw commands. Provider choice must not be prominent in the main workflow. | Audit the main UI for any visible provider dropdown, command composer framing, or terminal-first language. |
| WP-002 | 25-48 / P006-P011 | Auto-routing is required. The owner does not choose, toggle, or think about which AI is running. Kimi is the default for development/building/code/tests; Claude is the default for thinking/research/planning/review/explanation. Advanced override is the exception. | Default workflow must be automatic. Any manual route mode must be hidden or secondary, and copy must say orchestration rather than provider selection. |
| WP-003 | 49-88 / P012-P021 | Claude and Kimi must work together in one task feedback loop. Claude plans, owner approves, Kimi builds, Claude reviews, owner decides approve/change/rollback, and the loop can repeat. | A single-turn model is insufficient. The task thread must represent the planned multi-stage orchestration loop and persist each stage. |
| WP-004 | 91-123 / P022-P029 | The prior single-AI-per-request/provider-picker model is explicitly rejected as not what the owner wants. The portal, not the owner, orchestrates Claude/Kimi. | Implementation claims must not count a provider picker or one-shot chat as compliant orchestration. |
| WP-005 | 127-144 / P030-P034 | When a task starts, the owner describes what they want; Claude appears with a plan; after approval, Kimi automatically starts. Owner can use tags such as `#kimi` or `#claude` to override. | Task creation should lead to plan generation and approval. Tag overrides may exist, but should not replace automatic routing. |
| WP-006 | 149-182 / P035-P043 | Confirmed flow: owner request → Claude plan → approval → Kimi execution → Claude review → owner decision → optional change loop. | UI and backend state should have meaningful status labels/stages for plan, approval, build, review, decision, rollback/change loop. |
| WP-007 | 185-228 / P044-P054 | The conversation view must be one scrollable thread like Manus. CLI activity must be translated in real time to plain English using an inexpensive model. | No separate raw execution panel should dominate the default experience. Execution events should appear as translated thread updates. |
| WP-008 | 235-272 / P056-P065 | Layout decision: left sidebar section 1 has in-progress/executing tasks; left section 2 has memory categories Decisions, Features, Research; left section 3 has Skills, MCP, and API connections; right sidebar/drawer has a full collapsible cloud file system organized by the current task/chat. | Audit layout against this exact three-part left sidebar, center thread, and task-scoped right file system. |
| WP-009 | 277-306 / P066-P072 | The current task’s center thread and right file browser are part of a multi-task interface. The prior single-workspace model is explicitly described as a mismatch. | A three-panel page is not enough unless tasks can switch, memory is global, connections are visible/collapsible, and files are task-scoped. |
| WP-010 | 309-332 / P073-P078 | When switching tasks, the conversation and right file browser are per-task. A separate master view should show all files across all tasks, organized by task. | Audit whether task switching changes thread and file scope and whether any master file browser exists. |
| WP-011 | 335-403 / P079-P095 | Memory must be global, persistent, and accessible to every task. Files and conversations are per-task. Memory categories include Decisions, Features, Research, and Past Tasks. | A task-specific memory-only implementation is wrong. A global memory table/list is partial unless it supports the agreed categories and usability. |
| WP-012 | 409-444 / P096-P104 | The document declares the desired model a complete architectural redesign: left tasks/memory/connections, center per-task thread, right current-task files, Claude/Kimi loop, global memory, per-task files, and master files. | This is the baseline for implementation audit and gap classification. |
| WP-013 | 461-497 / P109-P117 | On new chat/task, the wrapper automatically connects to Kimi 2.6 and Opus in the background. Normal UI should never show a provider dropdown, never ask for Cloudflare credentials, and assume Claude/Kimi are connected. Exception is `#kimi`/`#claude` override. | There is an apparent later credential-dashboard refinement; normal task UI still must avoid provider setup and dropdowns. |
| WP-014 | 503-553 / P119-P130 | Clarified phrasing: “Never show a provider dropdown” in normal flow; a dropdown may exist for power users, but AUTO is default and correct most of the time. Best choice is orchestration, not provider selection. | The app must not surface provider selection as the primary action. If route selection remains, it should be Advanced or hidden. |
| WP-015 | 555-641 / P131-P151 | Required credentials are Anthropic/Claude API key, Cloudflare Account ID, and Cloudflare Workers AI API Token. One chat orchestration is required: plan, approve, Kimi build stream translated, Claude review, owner decision. | Credential status can exist, but the task experience remains a single translated thread. |
| WP-016 | 647-709 / P153-P167 | Corrected architecture is one wrapper task orchestrating two connected coding agents/tools. Kimi uses OpenCode configured with Cloudflare Workers AI for build/code/tests. Claude Opus 4.7 uses Anthropic/direct or OpenCode Anthropic route for planning/reasoning/review/summaries. The wrapper reads saved credentials securely. Optional defaults include model IDs and adaptive thinking/effort. | Backend architecture must separate planner/reviewer and executor responsibilities while presenting one task thread. |
| WP-017 | 711-750 / P168-P176 | Owner prefers Claude through native Anthropic API if it does not break the wrapper. Confirmed best route: Claude native Anthropic API; Kimi via OpenCode through Cloudflare Workers AI; wrapper orchestrates. | Current Claude direct API behavior aligns; Kimi should be evaluated for actual OpenCode/Cloudflare execution versus plain model invocation. |
| WP-018 | 751-799 / P177-P187 | Credentials are global for the whole workshop account. For one task there is normally one hidden OpenCode/Kimi execution session plus native Claude calls, not two visible terminals. The wrapper mediates; no user-visible terminal by default. | Terminal must be hidden by default. Credentials must be global, not per-task. |
| WP-019 | 801-809 / P188 | Credentials dashboard is owner-only. For now there is no user management. | Role/UI access should assume owner-only credentials; invited-user management should not be overbuilt in first version. |
| WP-020 | 813-836 / P189-P193 | If required credentials are missing or invalid, block the task immediately with plain-English setup guidance. Do not start limited mode. | Task start path must fail early and clearly when required credentials are absent. |
| WP-021 | 837-876 / P194-P202 | Missing/invalid credential flow should open the owner-only Credentials Dashboard. The chat should plainly explain the issue and route the owner to the dashboard. | Provider status banners are partial unless the dashboard-opening path exists or is represented clearly. |
| WP-022 | 879-916 / P203-P211 | Credentials Dashboard should be simple, without test buttons in the first version. It stores global owner-only credentials and startup checks whether values exist. Runtime failures should be translated to plain English and route to dashboard only if credential-related. | Do not require connection-test UI for compliance. Simpler presence-check status is acceptable for v1, with translated failure handling. |
| WP-023 | 919-971 / P212-P224 | First version should expose editable defaults if straightforward: Claude model `claude-opus-4-7`, adaptive thinking enabled, high or xhigh effort, and Kimi K2.6 through Cloudflare Workers AI. Target GitHub repo named `viyo-ai/AI-API-Web-Portal-v2` was identified in this historical decision record. | Current project may differ from the historical target repo. Model defaults and service-tier behavior must be audited without hardcoding misleading unsupported values. |
| WP-024 | 975-992 / P225-P227 | Since the GitHub repo was empty, initial push to `main` was approved in the historical build plan. | This is historical deployment/repo context, not necessarily a current instruction to push during this task. It should not override the current managed Manus project unless explicitly requested again. |
| WP-025 | 996-1009 / P228-P229 | The requested first build is a **real working orchestration MVP**. All decisions in the session about look and functionality must be followed 100%; otherwise scrap and start over. The system must be production ready, the wrapper LLM and UX/UI must be fully architected before code, and Standard Tier for Claude should be used. | Before code changes, produce architecture/requirement coverage. Audit must be honest about MVP versus prototype gaps. Claude API calls should be checked for service-tier support if implemented. |

## Cross-Document Clarifications and Tensions Noted During Ingestion

The WEBpORTAL document agrees with the original product vision that the owner must not see raw terminal output by default. It is even more explicit that the wrapper should usually run **one hidden OpenCode/Kimi execution session** plus native Claude API calls, with the wrapper mediating everything through the conversation thread. This reinforces the confirmed UI defect that a visible shell terminal in the default owner view is non-compliant.

The document contains an internal evolution around credentials. Earlier, lines 481-493 say the normal UI should never ask for Cloudflare credentials and should assume Claude/Kimi are already connected. Later, lines 555-916 define an owner-only global Credentials Dashboard and missing-credential blocking flow. The harmonized requirement is that **normal task UI must not ask for credentials or show provider setup**, but an **owner-only dashboard** may exist for global credential management and task startup must block if required credentials are missing.

The document also includes an evolution around provider selection. Lines 481-497 reject provider dropdowns in normal flow. Lines 503-553 allow a dropdown only for power users while keeping AUTO as the correct default. The harmonized requirement is that **orchestration, not provider selection, is the primary experience**, with any manual override demoted to an Advanced or tag-based path.

## Read Completeness Statement

This ingestion record was written after reading the extracted WEBpORTAL file completely from line 1 through line 1009. No code fixes, UI changes, or completeness claims should be made until the combined requirement register is created and the current implementation is audited against both ingested documents.
